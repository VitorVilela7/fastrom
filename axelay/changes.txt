Version 1.1:
 - Fixed the graphics decompression still reading the compressed data from SlowROM.
   The source bank is stored as a value, so the remapper could not see it.
 - Updated patch with asar 1.91 compatibility

Version 1.0:
 - First release.
